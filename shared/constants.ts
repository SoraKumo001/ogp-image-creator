/**
 * アプリ全体で共有する定数。
 * バックエンド・フロントエンド双方から参照する。
 */

export const DEFAULT_WIDTH = 1200;
export const DEFAULT_HEIGHT = 630;

/** レンダリング可能なサイズの上限（px） */
export const MAX_DIMENSION = 4096;
/** レンダリング可能なサイズの下限（px） */
export const MIN_DIMENSION = 120;
